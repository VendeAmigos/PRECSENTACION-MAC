---
export interface Props {
  title: string;
  description?: string;
}

const { title, description = "Linux Quest — Un RPG de trivia donde demuestras tu conocimiento de Linux derrotando al dragón del servidor" } = Astro.props;
---

<!doctype html>
<html lang="es" class="swiss-theme">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#0C0C0E" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Space+Grotesk:wght@400;500;600;700;800&family=Space+Mono:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet" />
    <link rel="preload" as="image" href="/assets/player.png" />
    <link rel="preload" as="image" href="/assets/player animation.gif" />
    <title>{title}</title>
  </head>
  <body class="swiss-grid-active theme-dark-swiss">
    <!-- Retícula de Guías Suizas (Grid Overlay) -->
    <div class="swiss-grid-overlay" aria-hidden="true">
      <div class="grid-col"></div>
      <div class="grid-col"></div>
      <div class="grid-col"></div>
      <div class="grid-col"></div>
      <div class="grid-col"></div>
      <div class="grid-col"></div>
    </div>

    <!-- Marcadores de Esquina Suizos -->
    <div class="swiss-corner-mark top-left">CH-1984 // LINUX QUEST</div>
    <div class="swiss-corner-mark top-right"><span class="swiss-plus">+</span> SWISS ARCHIVE TRIVIA</div>
    <div class="swiss-corner-mark bottom-left">SYSTEM.LINUX.01 // ARENA</div>
    <div class="swiss-corner-mark bottom-right"><span class="swiss-badge-red">CH</span> EDITION</div>

    <!-- Background Elements -->
    <div class="starfield" id="starfield"></div>

    <!-- Sound Toggle Button (Swiss Control Style) -->
    <button class="sound-toggle swiss-control-btn" id="sound-toggle" title="Alternar sonido">
      <span class="sound-icon-text">AUDIO: ON</span>
    </button>

    <slot />
  </body>
</html>

<style is:global>
  @import '../styles/global.css';
</style>
